sourceSets {
	main {
	  groovy {
		//exclude 'de/tud/stg/tigerseye/lang/logo/**' //TODO: include again after refactoring POPART project
	  }
	}
	test {
	  groovy {
		//exclude 'de/tud/stg/tigerseye/tests/logo/**' //TODO: include again after refactoring POPART project
	  }
	}
  }
  
dependencies {  
	implementation project(':core')
	implementation files("${rootDir}/lib/javalogo.jar") //Unfortunately, Java Logo is not available as an artifact
	implementation files("${rootDir}/lib/popart.jar") //TODO: include again after refactoring POPART project
		
	implementation libraries.groovy
	testImplementation libraries.junit
}